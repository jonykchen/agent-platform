# buf.yaml - Buf Proto 管理
# https://buf.build/docs/configuration/v2/buf-yaml

version: v2

modules:
  - path: contracts/proto
    name: buf.build/platform/agent

breaking:
  use:
    - FILE

lint:
  use:
    - DEFAULT
  except:
    - PACKAGE_VERSION_SUFFIX
    - SERVICE_SUFFIX