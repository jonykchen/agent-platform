"""Knowledge Service 主入口

【核心概念】Knowledge Service 在 RAG 架构中的位置
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Knowledge Service 是 RAG（Retrieval-Augmented Generation）架构的核心组件，
负责知识库的存储、索引和检索。

┌─────────────────────────────────────────────────────────────────────────────┐
│                         RAG 架构数据流                                       │
│                                                                             │
│   用户问题 → Orchestrator → Knowledge Service → Model Gateway → 响应        │
│                  │              │                    │                      │
│                  │              │                    │                      │
│                  │              ▼                    │                      │
│                  │        ┌─────────────┐            │                      │
│                  │        │  检索相关    │            │                      │
│                  │        │  知识片段    │            │                      │
│                  │        └─────────────┘            │                      │
│                  │              │                    │                      │
│                  └──────────────┴────────────────────┘                      │
│                                 ▼                                           │
│                     组装 Prompt + 上下文 → LLM 推理                          │
└─────────────────────────────────────────────────────────────────────────────┘

核心职责：
1. 文档管理：上传、存储、删除知识文档
2. 向量索引：将文档切分并生成 Embedding 存储
3. 语义检索：基于向量相似度检索相关内容
4. 混合检索：向量检索 + 关键词检索融合

【技术选型】向量数据库对比
┌─────────────────────────────────────────────────────────────────────────────┐
│  数据库       │  优点                          │  缺点                      │
├───────────────┼────────────────────────────────┼────────────────────────────┤
│  ✓ pgvector   │  PostgreSQL 原生扩展            │  超大规模性能不如专用方案  │
│               │  无需额外运维                   │  缺少高级索引（如 HNSW）   │
│               │  ACID 事务支持                  │                            │
│               │  成熟稳定                       │                            │
├───────────────┼────────────────────────────────┼────────────────────────────┤
│  Pinecone     │  托管服务、零运维               │  供应商锁定                │
│               │  高性能、自动扩展               │  国内访问受限               │
│               │  HNSW 索引                      │  成本较高                  │
├───────────────┼────────────────────────────────┼────────────────────────────┤
│  Milvus       │  高性能、分布式架构             │  运维复杂度高              │
│               │  支持多种索引（HNSW/IVF）        │  资源占用大                │
│               │  开源可控                       │  学习曲线陡峭              │
├───────────────┼────────────────────────────────┼────────────────────────────┤
│  Weaviate     │  语义理解强                      │  相对新、生态小            │
│               │  GraphQL API                    │  性能一般                  │
│               │  模块化架构                     │                            │
└─────────────────────────────────────────────────────────────────────────────┘

本项目选择 pgvector 的原因：
- 已有 PostgreSQL 基础设施，无需额外部署
- 数据量预估在百万级，pgvector 完全胜任
- ACID 保证数据一致性
- 运维成本低，团队熟悉度高

【服务拓扑】与其他服务的关系
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│   ┌────────────┐     gRPC/HTTP      ┌─────────────────┐                     │
│   │  Gateway   │ ──────────────────▶│  Orchestrator   │                     │
│   │  (Java)    │                    │   (Python)      │                     │
│   └────────────┘                    └────────┬────────┘                     │
│                                              │                              │
│                         ┌────────────────────┼────────────────────┐         │
│                         │                    │                    │         │
│                         ▼                    ▼                    ▼         │
│                 ┌──────────────┐    ┌──────────────┐    ┌──────────────┐   │
│                 │   Knowledge  │    │ Model Gateway│    │   Tool Bus   │   │
│                 │   (Python)   │    │   (Python)   │    │    (Java)    │   │
│                 │   Port:8003  │    │  Port:8002   │    │  Port:8083   │   │
│                 └──────────────┘    └──────────────┘    └──────────────┘   │
│                         │                                                    │
│                         ▼                                                    │
│                 ┌──────────────┐                                             │
│                 │ PostgreSQL   │                                             │
│                 │ + pgvector   │                                             │
│                 └──────────────┘                                             │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

调用关系：
- Orchestrator → Knowledge：Agent 需要外部知识时调用检索接口
- Knowledge → Model Gateway：生成 Embedding 时调用模型服务
- Knowledge → PostgreSQL：向量存储和检索

【API 端点列表】
┌─────────────────────────────────────────────────────────────────────────────┐
│  方法   │  端点                        │  功能                              │
├─────────┼──────────────────────────────┼────────────────────────────────────┤
│  POST   │  /api/v1/documents/upload    │  上传并处理文档                     │
│  GET    │  /api/v1/documents/{id}      │  获取文档信息                       │
│  DELETE │  /api/v1/documents/{id}      │  删除文档                          │
│  GET    │  /api/v1/documents/          │  列出文档列表                       │
├─────────┼──────────────────────────────┼────────────────────────────────────┤
│  POST   │  /api/v1/search/query        │  语义检索（支持混合检索）            │
│  POST   │  /api/v1/search/similar/{id} │  查找相似内容                       │
└─────────────────────────────────────────────────────────────────────────────┘

【扩展说明】如何添加新的文档处理器
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

步骤 1：在 processors/document_processor.py 中添加新处理器类

    class MarkdownProcessor(DocumentProcessor):
        '''Markdown 处理器'''

        async def extract_text(self, file_path: Path) -> str:
            # 实现文本提取逻辑
            import markdown
            md = markdown.Markdown()
            return md.convert(file_path.read_text())

        def supported_extensions(self) -> list[str]:
            return [".md", ".markdown"]

步骤 2：在 api/v1/documents.py 的 PROCESSORS 字典中注册

    PROCESSORS = {
        ".pdf": PDFProcessor(),
        ".docx": DocxProcessor(),
        ".md": MarkdownProcessor(),  # 新增
    }

步骤 3：（可选）如果需要特殊分块策略，修改 _chunk_text 函数

注意事项：
- 处理器必须是异步的（async def）
- 返回的文本应为纯文本格式，去除格式标记
- 大文件处理时注意内存使用，可流式处理

【参考】
- pgvector 文档: https://github.com/pgvector/pgvector
- FastAPI 官方文档: https://fastapi.tiangolo.com/
- RAG 最佳实践: https://python.langchain.com/docs/use_cases/question_answering/
"""

from fastapi import FastAPI

from app.api.v1 import documents, search


def create_app() -> FastAPI:
    """创建 FastAPI 应用

    【工厂函数模式】
    使用工厂函数而非全局 app 对象的原因：
    - 测试时可以创建多个独立实例
    - 支持多环境配置（dev/test/prod 不同 app）
    - 延迟初始化，避免导入时副作用

    Returns:
        配置好的 FastAPI 应用实例
    """
    app = FastAPI(
        title="Knowledge Service",
        description="RAG 知识库服务",
        version="1.0.0",
    )

    # ─────────────────────────────────────────────────────────────────────
    # 路由注册
    # ─────────────────────────────────────────────────────────────────────

    # 文档管理路由：上传、查询、删除文档
    app.include_router(documents.router, prefix="/api/v1/documents", tags=["documents"])

    # 检索路由：语义搜索、相似内容查找
    app.include_router(search.router, prefix="/api/v1/search", tags=["search"])

    return app


# ═══════════════════════════════════════════════════════════════════════════
# 应用实例 - 模块导入时创建
# ═══════════════════════════════════════════════════════════════════════════

app = create_app()
