"""
Core Module - 核心基础设施

【核心概念】
本模块提供服务级基础设施，包括配置管理、异常体系、常量定义等。
所有业务模块依赖此模块，但不依赖业务逻辑。

【模块职责】
┌──────────────────────────────────────────────────────────┐
│                     Core Module                          │
├──────────────────────────────────────────────────────────┤
│  config.py        配置管理（Pydantic Settings + 环境变量）  │
│  exceptions.py    异常体系（层级化错误码）                  │
│  constants.py     常量定义（状态枚举、错误码）               │
│  logging.py       日志配置（structlog JSON 格式）          │
│  dependencies.py  FastAPI 依赖注入                        │
└──────────────────────────────────────────────────────────┘

【技术选型对比】

| 组件 | 选型 | 备选方案 | 选型理由 |
|------|------|----------|----------|
| 配置管理 | Pydantic Settings | Dynaconf, python-dotenv | 类型安全，自动校验，IDE 友好 |
| 日志框架 | structlog | logging, loguru | 结构化 JSON 输出，支持上下文 |
| 异常设计 | 层级化错误码 | HTTP 状态码复用 | 细粒度错误分类，便于监控告警 |

【设计原则】
- 配置与代码分离：敏感信息通过环境变量注入
- 异常可观测：每类异常对应唯一错误码，便于日志分析
- 日志结构化：所有日志输出 JSON 格式，支持 ELK 索引

【依赖关系】
config.py ← exceptions.py ← logging.py
    ↑
    └─────── 所有业务模块
"""
